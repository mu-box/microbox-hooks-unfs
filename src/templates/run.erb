#!/bin/sh -e

# redirect stderr to stdout
exec 2>&1

export PATH="/asdf/data/sbin:/asdf/data/bin:/opt/gomicro/sbin:/opt/gomicro/bin:/opt/local/sbin:/opt/local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

sv start rpcbind || exit 1
exec chpst -u gomicro <%= exec %>
